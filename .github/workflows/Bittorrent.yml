name: Magnet to Rclone Direct Upload (BitTorrent Edition)

on:
  workflow_dispatch:
    inputs:
      magnet_link:
        description: 'Magnet Link'
        required: true
      remote_name:
        description: 'The name of the remote in your rclone.conf'
        required: true
        default: 'gdrive'

jobs:
  transfer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y transmission-cli rclone

      - name: Setup Rclone Config
        run: |
          echo "${{ secrets.RCLONE_CONF }}" > ./rclone.conf

      - name: Download via BitTorrent Engine
        run: |
          mkdir -p downloads
          echo "Starting BitTorrent Download..."
          
          # --seedratio 0 tells transmission to stop completely at 100% download
          transmission-cli --seedratio 0 -w ./downloads/ "${{ github.event.inputs.magnet_link }}"
          
          echo "Download finished successfully! Moving to upload step..."

      - name: Upload via Rclone
        run: |
          # This step will ONLY execute after transmission-cli completely exits at 100%
          rclone --config ./rclone.conf copy ./downloads/ "${{ github.event.inputs.remote_name }}:Downloads/" --progress
